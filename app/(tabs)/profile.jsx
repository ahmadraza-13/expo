import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const profile = () => {
  return (
   <SafeAreaView>
         <Text className="text-4xl text-center font-bold">Profile</Text>
       </SafeAreaView>
  )
}

export default profile