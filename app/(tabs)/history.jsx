import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const history = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl text-center font-bold">History</Text>
    </SafeAreaView>
  )
}

export default history