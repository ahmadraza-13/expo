import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const home = () => {
  return (
    <SafeAreaView>
      <Text className="text-4xl text-center font-bold">Home</Text>
    </SafeAreaView>
  )
}

export default home